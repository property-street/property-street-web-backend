# Connection to the ws endpoint
- If the user is an agent, add the endpoint to the connected_websockets and connected_agents_websockets set.
- on disconnect of a socket, the socket is removed from both set.